# Slack UAT 2026-03-15

Environment:
- Railway project: `free-slack-poll`
- Environment: `production`
- Service: `app`
- Deployment: `10a6e56e-6a3c-4d47-8dce-9dffdd2a4991`

Status legend:
- `PASS`
- `FAIL`
- `PENDING`

## Tests

1. `PASS` Slash command `/poll` opens the `Create poll` modal with the expected fields.
2. `PASS` Create a standard visible single-choice poll and verify the posted message updates after a vote.
3. `PASS` Create a poll with `Hide until poll closes` and verify results stay hidden until manual close.
4. `PASS` App Home loads and shows the poll management UI.
5. `PASS` Global shortcut `New poll` opens the create modal.
6. `PASS` Message shortcut `Create poll` opens the create modal from a message.
7. `PASS` App Home shows creator, created-time, and channel metadata for open polls after deployment `a4b398bb-269b-4d17-9d24-b50d787b3557`.
8. `PASS` App Home `Closed` filter and the poll details modal show the new metadata section, including closed timing for the closed UAT poll.
9. `PASS` Creating a new poll with a future scheduled close shows `Closes …` metadata in both App Home sections and on the posted poll message.
10. `PASS` Creating a visible single-choice poll without a scheduled close posts successfully, omits `Closes …`, and allows a vote change that preserves one participant.
11. `PASS` Creating an anonymous multi-choice poll with a scheduled close posts successfully, shows the new metadata in App Home, and accepts multiple selections from one participant.
12. `PASS` Creating a hidden-results poll from App Home keeps results hidden while open, then reveals the final counts after manual close.
13. `PASS` The poll details modal shows the `Metadata` section above the vote breakdown for both an open poll and a closed poll created during UAT.
14. `PASS` App Home `Closed` shows the newly closed poll with `Closed Today …` metadata after a Home refresh.

## Notes

- Start with end-user visible flows: modal entry points, poll posting, voting, result visibility, and App Home.
- Record any Slack-side errors or unexpected message text verbatim if a test fails.
- Test 2 initially failed because leaving `Close automatically` blank still triggered `The scheduled close time must be in the future.`
- Fixed in the workspace by normalizing blank datetime picker values to `null`, added a parser regression test, and redeployed Railway in deployment `9d3b20bf-f3bf-4bd0-9f6d-f0b78d4ef3af`.
- After the fix, a visible poll posted successfully and the shared message refreshed to `1 votes from 1 participant` after voting.
- Hidden-results flow passed: the open poll kept `Results are hidden until the poll closes.` after voting, then exposed the counts after `Close poll`.
- App Home initially failed because Slack rejected duplicate filter `action_id` values in the Home button row.
- Fixed by giving `Open` and `Closed` distinct action IDs, then redeployed Railway in deployment `10a6e56e-6a3c-4d47-8dce-9dffdd2a4991`.
- After the fix, App Home rendered `Polls`, `Create poll`, `Open`, `Closed`, `Your recent polls`, and `Polls you can manage`, and the `Closed` filter showed the closed UAT poll.
- Global shortcut passed: `Run shortcut` -> `New poll` opened the `Create poll` modal.
- Message shortcut passed: message `More actions` -> `Connect to apps` -> `Create poll` opened the `Create poll` modal and prefilled the source message text into the description.
- The creator-metadata UI was not visible on deployment `10a6e56e-6a3c-4d47-8dce-9dffdd2a4991`, which confirmed production was still on the older build.
- Manually deployed commit `2c27c1e` with `./scripts/railway-assist.sh deploy --verify none --ci`, producing Railway deployment `a4b398bb-269b-4d17-9d24-b50d787b3557`.
- After the new deploy, App Home rendered `Created by @Peter Ryszkiewicz`, `Created Today 6:21 PM`, and `Posted in #all-bright-builds-llc` for the open UAT poll.
- The `Closed` filter rendered `Closed Today 6:23 PM` for the closed UAT poll, and the `Poll details` modal also showed a `Metadata` section with creator, channel, and closed timing above the option vote breakdown.
- The Slack browser session hit the known `ssb/redirect` issue after login; forcing navigation to the workspace URL eventually recovered the authenticated app session.
- A newly created scheduled-close poll (`UAT closes metadata poll`) rendered `Closes Mar 17 9:00 AM` in both `Your recent polls` and `Polls you can manage`, and the posted message in `#all-bright-builds-llc` showed the same close timing.
- Additional basic UAT polls created:
  - `UAT basic visible single poll`
  - `UAT basic anonymous multi poll`
  - `UAT basic hidden results poll`
- `UAT basic visible single poll` posted without a `Closes …` line, updated from `Alpha 1 votes • 100%` to `Beta 1 votes • 100%` after a vote change, and closed successfully from the manager action.
- Clicking a vote button on the closed visible poll did not change the totals, which stayed at `1 votes from 1 participant`.
- `UAT basic anonymous multi poll` posted as `Anonymous poll`, `Multiple choice`, and `Closes Mar 17 9:00 AM`; voting both options updated it to `2 votes from 1 participant`.
- The App Home `Open` view did not refresh automatically after manually closing `UAT basic visible single poll`; it continued to show stale open-state data until the Home view was refreshed or the filter was toggled.
- After switching App Home to `Closed`, the newly closed poll appeared in both sections with `Closed Today 7:30 PM`.
- `UAT basic hidden results poll` was created from the App Home `Create poll` button by selecting `#all-bright-builds-llc` in `Post in conversation` and setting results visibility to `Hide until poll closes`.
- While the hidden-results poll was open, the message continued to show `Results are hidden until the poll closes.` after voting.
- After closing the hidden-results poll, the posted message revealed `Alpha 1 votes • 100%` and the details modal showed `Closed Today 7:34 PM` in the `Metadata` section.
