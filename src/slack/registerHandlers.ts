import type { App } from "@slack/bolt";

/**
 * Registers Slack handlers on the Bolt app instance.
 */
export function registerHandlers(_app: App) {
  // Handlers are wired in later implementation phases.
}
